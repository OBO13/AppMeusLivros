<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.appmeuslivros">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Remove_livro_toc_tela"></activity>
        <activity android:name=".LivroAdapter2" />
        <activity android:name=".PesquisarLivro" />
        <activity android:name=".TodosLivros" />
        <activity android:name=".Dialogo" />
        <activity android:name=".ListaLivros" />
        <activity android:name=".CadastrarLivro" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>